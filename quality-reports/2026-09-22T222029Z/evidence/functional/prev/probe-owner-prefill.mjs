import { chromium } from '@playwright/test';
const b = await chromium.launch(); const p = await b.newPage();
await p.goto('http://localhost:5173/login');
await p.getByLabel('Username').fill('buggy'); await p.getByLabel('Password').fill('1970beetle');
await p.getByRole('button',{name:'Login'}).click(); await p.waitForURL(/board/);
await p.getByRole('button',{name:'New Bug'}).click();
const d = p.getByRole('dialog',{name:'Create bug'});
await d.getByLabel('Owner').fill('');
console.log('immediately', await d.getByLabel('Owner').inputValue());
await p.waitForTimeout(500);
console.log('after 500ms', await d.getByLabel('Owner').inputValue());
await d.getByRole('button',{name:'Save'}).click();
console.log('errors', await d.getByRole('alert').textContent());
await p.getByRole('button',{name:'Cancel'}).click();
await p.getByRole('button',{name:'New Bug'}).click();
await p.waitForTimeout(500);
await d.getByLabel('Owner').fill('');
await d.getByRole('button',{name:'Save'}).click();
console.log('after wait then clear, errors', await d.getByRole('alert').textContent());
await b.close();
